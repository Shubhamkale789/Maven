version: 0.2

phases:
  install:
    commands:
      - echo Installing Maven dependencies...
      - mvn install
  build:
    commands:
      - echo Building the project...
      - mvn package
artifacts:
  files:
    - target/*.jar
